import { Routes } from '@angular/router';
import { AzureLoginComponent } from './feature/azure-login/page/azure-login.page';

export const routes: Routes = [{ path: '', component: AzureLoginComponent }];
